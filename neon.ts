import { defineConfig } from "@neon/config/v1";

// Neon Infrastructure as Code configuration
// Org: org-falling-sea-64731952
// Project: flat-field-51671776
export default defineConfig({
  // Lakebase Postgres is enabled by default for the project
});
